# burr

## Trigger

The `burr` Debt marks a function parameter whose underscore spelling says it is accepted but unused.

## Move

Read the function body and every call site before editing. If the work belongs to the callee, consume the value and remove any redundant sibling parameter. If the caller over-declared the contract, delete the parameter honestly. Never sweep underscore parameters mechanically.

## Evidence

The keel query/run specimen used the same `_plan` symptom for opposite causes; local classification removed leaked responsibility and reduced a six-parameter boundary to four.

## EXIT

Remove this entry when the burr verdict becomes mechanical or the burr Debt retires.
