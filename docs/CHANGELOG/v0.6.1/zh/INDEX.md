# Ectropy v0.6.1

本版继承 Plumb v0.18.21，因此 `ectropy skill` 发现 Grok 的方式和发现
Claude、Codex 一样，受管 brief 装在 `~/.grok/skills/ectropy`。

所有权、ledger、force、upgrade、uninstall 不变。路径未入账时，即使有
匹配的 marker 也仍然拒绝覆盖。

本版的受管文档面是 `AGENTS.md`、`DESIGN.md` 和 ectropy brief。v0.6.0
之后已经在 main 上把原先散落的文档折进去了。

十六条法和 `ectropy law` 没有变更。
