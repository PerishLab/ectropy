# Migrating to Ectropy v0.7.1

Nothing to migrate. A repository that reported `coverage` on a Svelte directive
binding a custom property will report it no longer; no other finding changes.

No `ectropy.toml` schema migration is required.
