# Ectropy v0.7.1

Svelte 的属性名现在可以同时携带指令前缀与自定义属性名。`style:--ruled={value}`
以及任何指令上的同种写法，此前都是扫描读不到的地形，于是 `coverage` 法则拒绝
该文件——这是对的，因为从未解析过的地面无法被 clean 覆盖。

属性名此前只允许每个标识符前有一个分隔符。分隔符现在可以重复，于是指令前缀、
data 属性、事件修饰符与自定义属性名走同一条规则。

法则目录与配置 schema 没有变化。
